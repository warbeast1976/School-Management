/**
 * Laravel API (Herd: link BE folder as school.test).
 * UI runs only from this FE folder (e.g. fe.test in Herd).
 */
export const API_BASE_URL = 'https://school.test/api';

export const TOKEN_KEY = 'ar_portal_token';
export const USER_KEY = 'ar_portal_user';
